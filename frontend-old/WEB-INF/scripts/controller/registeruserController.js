define([
    'jquery',
    'angular',
    '../../app/app.js',
    '../../model/user.js'
], function ($, angular, proShopApp, user) {

    //proShopApp.controller('registeruserController', function registeruserController($scope) {
    //    alert(user.name);
    //});
});